# The KDCube platform integration: the code that binds the vendored solution/
# agent to the platform — identity (per-user isolation mapping) and stream_adapter
# (the graph's event stream -> the chat component via comm_ctx). entrypoint.py
# composes these; solution/ is the agent itself.
