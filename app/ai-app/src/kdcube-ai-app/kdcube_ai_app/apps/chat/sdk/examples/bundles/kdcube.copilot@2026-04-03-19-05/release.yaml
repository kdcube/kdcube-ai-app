bundle:
  repo: "https://github.com/kdcube/kdcube-ai-app.git"
  ref: "2026.5.16.407"
  description: |
    User-memory, Telegram WebApp, economics, and telemetry sink release for kdcube.copilot.

    Highlights

    - Derives the Copilot bundle from the memory + economics entrypoint stack so
      the bundle can expose durable user memory, cost accounting, and the normal
      ReAct workflow in one surface.
    - Adds a single `copilot_webapp` source-folder widget with two sections:
      User Memory and Telegram Admin. It works as a normal KDCube widget and as
      the Telegram Mini App entrypoint.
    - Reuses the shared memory widget source directly inside the Copilot WebApp
      build through `shared_sources`, avoiding iframe composition.
    - Adds public Telegram WebApp routes for memory operations and Telegram user
      administration, scoped through verified Telegram `initData`.
    - Adds shared Telegram WebApp admin/channel components to the Copilot widget
      so Telegram users can request access, admins can approve users, and
      approved users can manage Telegram conversations from the same WebApp.
    - Resolves Telegram chat submission through the mounted runtime bundle id
      from comm/request context, avoiding `unknown_bundle` when descriptor ids
      differ from the bundle's short module id.
    - Wires memory tools into the Copilot ReAct tool descriptor so configured
      agents can search/read and, when policy allows, write durable user memory.
    - Keeps the economics pre-run hook compatible with both legacy
      `pre_run_hook(state=...)` subclasses and newer hooks that accept
      `econ_ctx`.
    - Adds the SDK stats telemetry sink wiring for copilot ReAct turns and
      doc-reader MCP calls. The sink uses a configured POST endpoint plus
      bearer token and does not write local telemetry files.
    - Adds bundle-local config templates for knowledge, MCP auth, Telegram,
      `copilot_webapp`, and `telemetry_sink.endpoint_url`.

    Known follow-ups

    - Validate the Copilot Telegram WebApp with a real BotFather WebApp button
      and deployed public origin.
    - Validate telemetry ingestion against the selected stats receiver endpoint
      in the deployed demo environment.
