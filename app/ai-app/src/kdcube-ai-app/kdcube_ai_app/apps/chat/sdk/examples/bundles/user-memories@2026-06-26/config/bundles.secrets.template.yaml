bundles:
  version: "1"
  items:
    - id: "user-memories@2026-06-26"
      # This app holds no secrets of its own. The user-memory store uses the
      # platform database; embeddings/economics use platform-scoped credentials.
      # Keep this file as the (empty) secrets contract so deployments stay
      # consistent; add keys here only if this app gains its own integrations.
      secrets: {}
