Author self-contained SVG diagrams with explicit viewBox, containment math, and reusable markers.
- Use SVG when Mermaid is too restrictive for custom layout, nested containers, badges, or mixed arrow styles.
- Validate each diagram with write_png using `format='html'`, `content_selector='svg'`, and `fit='content'`.
- Ensure all boxes, pills, and labels fit inside parent containers before finalizing.
