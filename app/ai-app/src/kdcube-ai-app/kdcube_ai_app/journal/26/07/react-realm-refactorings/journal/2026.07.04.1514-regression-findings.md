What I found so far:

1. Download failure is clear
   The canvas object action resolved conv:fi:... through the conversation resolver, but its capabilities say:

preview=true, open=true, download=false

So the UI asks download, resolver rejects it with unsupported_object_action.

Concrete log line:

[canvas.object_action] requested action=download namespace=conv ref=conv:fi:...
[canvas.object_action] failed ... capabilities={'preview': True, 'open': True, 'download': False, 'rehost': False}

That is a real missing integration: conversation conv:fi object actions need download support when the artifact has bytes / can be materialized.

2. Exec retry issue is likely in the exec harness/path normalizer
   I found the path-rewrite area in exec_tools.py:

- normalize_exec_contract_for_turn
- _split_turn_artifact_path
- code rewriting around relative git/projects/, files/, attachments/
- contract normalization messages still say rewritten to current turn files paths.

Your suspicion is plausible: the generated code correctly wrote under OUTPUT_DIR / turn_.../git/projects/..., but the harness likely normalized or validated contract/code paths
using old assumptions and caused a doubled path.

I have not edited yet. Next I will make a small fix in two places:

- add download handling for conversation conv:fi object action;
- fix exec contract/code path normalization so already-qualified turn_<id>/git/projects/... and turn_<id>/files/... paths are left intact and not rewritten/doubled.


---
Elena: what is canvas_object_action should be probably be scene_object_action so actionable for all elemenets on scene (conencted to this bundle where we have this config_

---

fi: today does not normally return base64. The client type supports content_base64 as a fallback, but the fi: resolver returns a download_url. I kept conv:fi aligned with that: no
base64, it now returns /api/cb/resources/.../download.

Changed:

- app/ai-app/src/kdcube-ai-app/kdcube_ai_app/apps/chat/sdk/tools/exec_tools.py: nested current-turn paths like turn_.../files/expense_tracker/demo_output.txt and turn_.../git/
  projects/... are now recognized as already-qualified, so they are not rewritten/doubled.

- app/ai-app/src/kdcube-ai-app/kdcube_ai_app/apps/chat/sdk/solutions/chat/events/resolver.py: conv:fi: is now treated as a conversation file object, not as conv:<conversation>, and
  download returns a resource URL.

- app/ai-app/src/kdcube-ai-app/kdcube_ai_app/apps/chat/sdk/examples/bundles/versatile@2026-03-31-13-36/services/canvas.py: passes tenant/project into the conversation resolver so it
  can build the resource URL.

- Added focused tests for both regressions.

About canvas_object_action: yes, semantically this is misnamed now. It is acting as a generic object-action endpoint for scene/chat/canvas-connected objects. A better name would be
scene_object_action or bundle_object_action. I did not rename the route in this patch because that is a wider client/API cleanup; this fix keeps the current route but corrects the
behavior.

---


The model did produce the long code, but it produced it as raw text before the first valid <channel:...> block. Our debug logger records full_raw, so you see it in proc logs. The
ReAct streamer only materializes content inside <channel:thinking>, <channel:action>, <channel:code>, etc., so that pre-channel code was neither UI thinking nor executable code.
Then the model opened the real <channel:code> late, after burning tokens, and that actual code channel ended at else.

I fixed this in two places:

- app/ai-app/src/kdcube-ai-app/kdcube_ai_app/apps/chat/sdk/solutions/react/v3/agents/decision.py:212: detect raw output before the first valid ReAct channel and mark the round
  invalid.

- app/ai-app/src/kdcube-ai-app/kdcube_ai_app/apps/chat/sdk/solutions/react/v3/runtime.py:1197: convert that into a protocol violation/retry, so no partial tool action runs.
- app/ai-app/src/kdcube-ai-app/kdcube_ai_app/apps/chat/sdk/solutions/react/v3/agents/decision.py:302: tightened protocol instructions: only write inside <channel:...> blocks; any
  outside text is dropped, and channel:thinking must not contain code/HTML/long drafts.

- app/ai-app/src/kdcube-ai-app/kdcube_ai_app/apps/chat/sdk/tools/exec_tools.py:591: fixed path rewriting so code like OUT / TURN / "files/..." is not rewritten into OUT / TURN /
  "turn_.../files/...". This now uses only a local assignment fact like TURN = "<current turn id>", not invented variable-name semantics.

---

