# Postgres setup job (one-shot)
# Used when bootstrapping schemas on a managed Postgres instance.

# Required: shared gateway config (tenant + project)
# Example:
# GATEWAY_CONFIG_JSON='{"tenant":"<TENANT_ID>","project":"<PROJECT_ID>","profile":"development"}'
GATEWAY_CONFIG_JSON=''

POSTGRES_HOST="<POSTGRES_HOST>"  # managed DB or local-infra-stack (localhost)
POSTGRES_USER="<POSTGRES_USER>"
POSTGRES_PASSWORD="<POSTGRES_PASSWORD>"
POSTGRES_DATABASE=kdcube
POSTGRES_PORT=5432
# Enable if your managed Postgres requires SSL
POSTGRES_SSL=False

# For Postgres container (if needed by tooling)
POSTGRES_DB="${POSTGRES_DATABASE}"
PGPORT="${POSTGRES_PORT}"

# Optional: override admin defaults used by the bootstrap script
# ADMIN_USER_ID=
# ADMIN_USER_EMAIL=
# ADMIN_USER_NAME=
