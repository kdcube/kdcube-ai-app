platform:
  repo: "kdcube-ai-app"
  ref: "2026.3.16.2245"
  description: |
    Fix asyncpg Postgres pool configuration so POSTGRES_SSL=true maps to sslmode=require by default, add explicit POSTGRES_SSL_MODE/POSTGRES_SSL_ROOT_CERT support, and prevent ingress/
    proc schedulers from failing on RDS certificate verification unexpectedly
  config:
    version: "2026.3.16.2245"